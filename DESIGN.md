# Vroma Cut 初期設計

2026-09-11。初期設計案。
既存コードと共有仕様を確認した範囲、および実ファイルでの検証が必要な範囲を区別する。
添付の実装指示書は構想資料として扱い、このセッションの成果物は設計とする。

## アプリの役割

Vromaで走行中に残したマークから、撮影後に残したい場面を見つけ、映像を確認して必要な長さに整え、撮影時刻を維持したMP4として保存する。
長時間の素材を最初から見直す負担と、分割ファイルを手で選んで結合する作業を減らす。
出力はChrovaで元のGPXと対応できることを目指す。
ユーザーが想定する出力後の主用途はFinal Cut Proでの編集、または動画の直接参照である。
独自データは未加工の別ファイルに退避する。

Vromaは記録とマーク、Vroma Cutは素材の選別と無劣化の切り出し、Chrovaは保存したメディアの整理を担う。
VTouchで確認した「内部日時を変更し、読み戻して確かめる」という考え方を採用する。
既存アプリの画面構成や書き出し実装をそのまま移植することは設計の前提にしない。

初期対象はmacOS上のAce Pro 2の通常のMP4とする。
GPXやマークがなくても手動で切り出せる。
カメラ操作、色調整、字幕、トランジション、複数カメラの同期、再エンコードによるフレーム単位の切り出しは初期版に含めない。

## 既存コードから確認したこと

| 対象 | 確認した挙動 | 設計への反映 |
| --- | --- | --- |
| Vromaのマーク | `.marks.csv`の第1列はUTCのISO 8601、第2列は表示用の現地時刻、以降はコメント。小数秒あり／なしを受け入れる | 第1列を対応計算に使い、第2列から時刻を再計算しない |
| VromaのGPX | GPX 1.1。各`trkpt`が時刻を持つ。`metadata/time`は別に存在する | マークとの対応は絶対時刻で行い、GPXの開始を動画の開始とみなさない |
| Chrovaの動画読込 | `AVAsset.creationDate`の`dateValue`を撮影時刻として読む。インポートで取得できない場合は元ファイルの作成日時へフォールバックする | 正しい内部日時を必須とし、ファイル日時だけで成功判定しない |
| ChrovaのGPX対応 | 撮影時刻以前の最新の測位点を候補とする。GPSのないメディアに適用する | 元のGPXを利用できる。GPSのある動画とない動画を分けて連携検証する |
| Chrovaの期間検索 | 撮影時刻をGPXの開始／終了に対して比較する | 出力開始がGPXの記録期間外なら、その事実を表示する |
| VTouch | 内部日時をパススルー書き出しで変更し、AVFoundationで読み戻す。Finderの作成日時と変更日時も設定する | 日時の検証方法を参考にする。追加ストリームの保持まで同じ実装で保証できるとはみなさない |

Vromaのサンプルには、`2026-02-10T01:48:06.999Z`と`2026-02-10T10:48:06.999+09:00`が同じ行に記録されている。
秒だけに丸めず、元の小数秒を保持する。
既存CSVの書き手は改行を含むコメントを引用できるが、確認した読み手は行ごとに分割するため、この実装を無条件に再利用しない。
Vroma Cutの読み手は引用された改行も扱い、既存の実サンプルとの互換性をテストする。

goprofilemergeandcopyについては、ユーザーの追加指示によりコード調査を行わない。
FFmpegを実行バイナリとして呼び出す方式ではなく、ライブラリとして使う点だけを今回の設計に反映する。

## 最初から書き出しまでの操作

1. 動画フォルダを開く。GPXとマークは同じ場所にあれば候補を表示し、別の場所からも追加できる。
2. サイドバーで録画を選ぶ。分割ファイルは録画1本として並べ、撮影時刻、長さ、候補クリップ数を表示する。
3. マーク由来の候補を順に確認する。既定範囲は10秒前から60秒後とし、開始点と終了点を調整する。
4. 時刻が合っていなければ、映像の位置とマークを指定して合わせる。補正後の候補範囲を確認する。
5. 残す候補を選んで書き出す。書き出し前に実際の範囲と日時を表示する。
6. 完了したクリップを同じ画面から再生し、Finderで表示できる。

初回に全項目の設定を要求しない。
動画が読めた時点で再生と手動範囲の編集を可能にし、解析は録画単位で進める。
時刻の解釈が不明な動画は、再生できてもマークの自動割当と書き出しを確定させない。

## 画面と編集の仕様

1ウィンドウを、録画一覧、ビューアーとタイムライン、候補クリップ一覧で構成する。
タイムラインは上からマーク、選択範囲、動画のサムネイルを配置する。
ファイル境界は控えめな区切りとして示し、欠落や非互換な境界だけを目立たせる。
地図はGPXがあるときに開く補助表示とし、動画の確認領域を常に圧迫しない。

編集対象は「再生位置」「開始点」「終了点」のいずれかを明示する。
左右キーはフォーカス中の対象を1秒動かす。
開始点または終了点を選んだ場合、ビューアーもその位置を表示する。
テキスト入力中はキーを奪わない。
前後1秒、10秒、60秒のボタンも同じ対象に作用させ、対象名を操作部に表示する。
開始点と終了点の逆転を許さず、Undoで範囲変更、削除、結合、補正変更を戻せるようにする。

再生位置から「範囲を追加」すると、前10秒から後60秒を初期値とする手動候補を作る。
重なる候補は既定で個別に残す。
マークの意味を保ったまま確認できるためであり、これはVroma Cutの設計上の選択である。
利用者が選択した候補には「1つにまとめる」を用意し、元のマークとの関連を残す。

候補の生成は再読込のたびに繰り返さず、同じマークから重複作成しない。
時刻補正を変えるとマークは移動する。
未編集のマーク由来候補は追従し、編集済み候補と手動候補は動画上の位置を保つ。
追従しなかった候補には「時刻合わせ後に未確認」を表示し、再確認できるようにする。

## 時刻モデル

動画上の経過時間と撮影の絶対時刻を別に持つ。
動画上の時間は整数値とtimescaleの組で表し、フレーム数や浮動小数の秒数の累積を基準にしない。
絶対時刻はUTCで保存し、カメラの現地時刻を解釈したタイムゾーンと根拠も保持する。

```text
C = カメラの録画開始日時を明示したタイムゾーンで解釈したUTC時刻
Δ = カメラ時刻に加える補正値
t = 動画の先頭表示フレームを0とした経過時間
T(t) = C + Δ + t
マークの位置 t = マークUTC時刻 - (C + Δ)
位置合わせの補正 Δ = マークUTC時刻 - (C + 選択した動画位置)
```

補正のUIは「カメラ 14:03:20 → Vroma 14:05:00」「カメラ時刻に +00:01:40」と表示する。
日付をまたぐときは両方の日付も示す。
この例では14:05:30のマークが動画開始から30秒に対応する。
タイムゾーンと時計のずれは別の設定とし、編集中の状態にMacの現在のタイムゾーンを後から適用しない。

補正値はプロジェクトのカメラ単位で保持し、録画ごとの上書きも可能にする。
一定のずれを初期版の対象とし、長時間の時計の進み遅れを直線補間する機能は追加しない。
複数の位置合わせが一致しない場合は、その差を表示して一定補正の限界を確認できるようにする。

ファイル名の日時にはタイムゾーンが含まれない。
内部日時とファイル名のどちらが正しい録画開始を表すかはAce Pro 2の実ファイルで確定する。
両方を読み、解釈が異なる場合は根拠と候補を示す。
Finderの日時やGPX開始時刻を自動的に録画開始の代用にしない。

マークが録画範囲外なら自動候補を作らず、「対応する録画なし」に残す。
範囲内のマークに対してだけ、初期範囲を録画先頭と末尾で丸める。
半開区間`[開始, 終了)`を内部規約にし、隣接区間の二重所属を避ける。

## 分割録画のモデル

添付にある`VID_YYYYMMDD_HHMMSS_連番.mp4`はグルーピングの候補規則とする。
親フォルダと日時部分が同じファイルを候補としてまとめ、連番を数値順に並べる。
桁数を固定せず、先頭番号が0であることも要求しない。
先頭や末尾の不足はファイル名だけでは判断できないため、「欠番なし」と「録画が完全」を同義にしない。
別フォルダにある同名録画を自動で混ぜない。

論理動画は順序を持つ物理セグメントと、各セグメントの表示時間、絶対時刻の根拠、境界の検証結果を持つ。
コンテナのdurationだけを足して同期を確定せず、映像のPTS、音声との位置関係、編集リストを調査して共通の時間対応を作る。
プレビューと書き出しは同じ対応表を使う。

欠番を検出した場合、欠けた時間を詰めて一本の連続動画として扱わない。
欠落後の時刻を独立に確定できなければ、その区間のマーク対応も未確定にする。
欠番や非互換境界をまたぐ書き出しは止め、問題のない連続区間は独立して利用できるようにする。

互換性は全ストリームの種類、コーデック、初期化情報、time base、映像寸法、色と回転の情報、音声レイアウトなどで確認する。
比較差分は記録し、番号や解像度が同じという理由だけで互換と判断しない。

## 無劣化の範囲と出力時刻

利用者が選んだ**希望範囲**と、コピー可能な**出力予定範囲**、検証で得た**実出力範囲**を別々に保存する。
範囲を調整しても、希望範囲をキーフレームへ勝手に置き換えない。
タイムラインに出力予定範囲を重ね、増える前後の秒数を表示する。

初期方針は、希望範囲を含むデコード可能な区間へ広げる方式とする。
開始は直前の独立して再生できる地点を選び、終了も参照フレームの依存を満たす境界にする。
キーフレームフラグだけで独立再生可能と断定せず、HEVCの先行画像、Bフレーム、音声パケット境界を検証する。
安全な範囲を確定できない入力は、黙って一部を欠いた出力にしない。

出力日時は`T(実際に最初に表示される映像の元動画位置)`とする。
最小DTSや単純な出力PTSの0を、そのまま撮影時刻として使わない。
出力の時刻が0へ移された場合にも、元動画の位置との対応を結果に残す。
希望開始からの差と希望終了からの差を、出力後にも確認できるようにする。

FFmpegの公式文書は、stream copyのシークで指定位置より前の区間が残り得ることを説明している。
CLIのオプション順を試して得た結果を、そのままlibav実装の保証として扱わない。
concatについても同一ストリーム構成が条件であり、各ストリームの長さの差は隙間を生む場合がある。
実装はこの制約を検査する。[FFmpegのシーク仕様](https://ffmpeg.org/ffmpeg.html#Main-options)、[concatの仕様](https://ffmpeg.org/ffmpeg-formats.html#concat)

## メディアエンジン

macOSのUIはSwiftUIを基本とし、細かなフォーカス制御とファイル操作にはAppKitを使う構成を提案する。
製品の実装言語はSwiftを基本とする。
`experiments`内のMJSは調査と実証専用であり、製品にNode.jsを同梱したり実行したりする構成にはしない。
独自領域の退避、ハッシュ検証、来歴保存もSwiftで実装し、libavへの接続だけを薄いCインターフェースとする。
プレビューはAVFoundationを第一候補とするが、分割境界での再生とエンジンの時間対応が一致することを実ファイルで検証して採用する。
初期のOS下限はVTouchと同じmacOS 15を提案し、対応機種と配布方針に合わせて確定する。

書き出しはlibavformat、libavcodec、libavutilをライブラリとして使い、薄いCインターフェースで包む構成とする。
Swift側にはFFmpegのポインタや構造体を公開せず、解析結果、書き出し計画、進捗、結果だけを渡す。
全録画を一度結合する巨大な中間ファイルを作らず、必要な物理セグメントから1つの出力へパケットを送る。
日時変更のために、完成した動画をさらにAVFoundationで書き出し直す構成にはしない。

アプリ本体に直接組み込む案と専用の補助プロセスにリンクする案は、実験コードの結果を踏まえて決める。
初期設計では後者を推奨する。
メディア解析の失敗やキャンセルをUIから分離できるためである。
プロセス境界のデータ量は計画と進捗に限定し、映像データ自体はファイルから処理する。
ファイルへの権限受け渡し、署名、同梱ライブラリ構成の検証を採用条件にする。
CLI版FFmpegは開発時の比較検証に利用し、利用者にHomebrewや外部コマンドの設定を求めない。

エンジンは全ストリームを列挙し、保存先との対応表を作る。
Ace Pro 2の実サンプルにはHEVC、AAC、`tmcd`の3トラックがある。
FFmpeg 8.0のMP4 muxerでは、この`tmcd`をそのままコピーする試験がヘッダー作成時に失敗する。
MP4 muxerによるタイムコード再生成は可能だが、元のトラックの単純コピーとは区別する。
映像と音声はパケットをコピーし、タイムコードについては時刻の意味と切り出し位置を保って再構成する案を検証対象とする。
試験コードでは元の約30 fpsのカウンタを映像の約60 fpsに換算しているため、元のトラック表現まで同一とみなさない。
扱えないストリームを検出した場合は、種類を示してその書き出しを停止する。
ストリーム数が一致するだけでは保持を証明できないため、選択区間のパケット内容、時刻、メタデータ、dispositionも確認する。
コンテナ固有のboxやメーカーの独自領域はストリームとは別に調査する。
未知の領域を無条件に複写すると古いオフセットを残す可能性があるため、実ファイル調査の前に「全情報を保持」と表現しない。

提供サンプルでは、末尾の`inst` boxが53,603,811 bytesあり、3トラックとは別に存在する。
中には慣性センサーの時系列、露出、プレビュー、機種情報、未解釈の記録が入っている。
FFmpegによるトラックのコピーとメタデータ辞書のコピーだけでは、このboxを保持できない。
構造と参照先は[サンプルのメディア構造](experiments/SAMPLE_FORMAT.md)に記載する。

独自領域は、元の物理MP4ごとに1回だけ、出力先の`SourceMetadata`配下へ未加工で退避する。
1本から複数クリップを作る場合も同じ退避データを共有する。
独自形式の切り詰めや、短いMP4への埋め戻しは初期版では行わない。
利用者が明示的に選択した退避方式であり、切り出したMP4だけで元データをすべて保持する要件とは区別する。

将来の解析に備え、`inst`単体だけでなく、原本の`moov`、`ftyp`を含む全トップレベルboxを保存する。
ただし`mdat`はヘッダーだけとし、動画や音声などのサンプル本体を含めない。
これにより`moov/udta`にある別の独自情報や、元のトラック目次も未加工で残せる。
元MP4全体のバックアップや、元のメディアを復元できる形式とは表示しない。

原本のSHA-256を退避単位の識別子に使う。
同名でも内容が違うMP4を混同せず、同じ内容を再度書き出した場合は検証済みの退避データを再利用する。
保存する各boxには順序、種類、元ファイル内のオフセット、元の長さ、退避した長さ、SHA-256を付ける。
解釈できないboxも内容を書き替えず保存し、構造上の境界を読めないファイルは不完全な退避を成功扱いしない。

```text
出力先/
  vroma_20260911_110734.mp4
  ExportManifest.json
  SourceMetadata/
    <原本SHA-256>/
      manifest.json
      0000-66747970.bin    ftypの全体
      0001-6d646174.bin    mdatのヘッダーのみ
      0002-6d6f6f76.bin    moovの全体
      0003-696e7374.bin    instの全体
```

ファイル名の16進数はboxの4-byte種別をそのまま表し、任意の種別を安全に表現するために使う。
利用者には画面で「元動画の追加データ」と表示する。
`ExportManifest.json`には各クリップの原本SHA-256、参照する退避フォルダの相対パス、希望範囲、実出力範囲、元のPTSとの対応、時刻補正、タイムゾーンを記録する。
分割境界をまたぐクリップは、関係するすべての原本と各原本内の範囲を列挙する。
AACフレームの長さと映像の分割点が一致しない場合、境界直前の音声サンプルのMP4上の長さは次のサンプルのDTSとの差になる。
圧縮内容とPTS/DTSを維持し、この境界の間隔を計算した上で出力時刻のハッシュを照合する。変更した境界数は`audioBoundaryAdjustments`に記録する。
これは解析と来歴保存のための付属ファイルであり、Final Cut ProやChrovaに読み込みを要求しない。

退避後に原本の該当領域とハッシュを照合し、一致した場合だけ完了として確定する。
退避できなかった場合はクリップを「追加データの保存が未完了」とし、再試行できるようにする。
原本は変更せず、自動削除もしない。
出力を別の場所へ移す操作では、関連する退避フォルダとmanifestも含める。

FFmpegの公式remux例は音声、映像、字幕以外を除外しているため、そのまま使用すると今回の追加ストリーム保持要件を満たさない。
これはサンプルコードの挙動であり、FFmpeg全体がデータストリームを扱えないという意味ではない。[公式remux例](https://ffmpeg.org/doxygen/trunk/remux_8c-example.html)

## ファイル名とGPXの受け渡し

ファイル名は`vroma_YYYYMMDD_HHMMSS.mp4`を採用する案とする。
日時は補正済みの実出力開始時刻を、プロジェクトに保存した表示タイムゾーンで表す。
小数秒はファイル名では切り捨て、内部モデルと検証結果には保持する。
同名は`vroma_01_YYYYMMDD_HHMMSS.mp4`、`vroma_02_YYYYMMDD_HHMMSS.mp4`の順に回避する。
番号の最大桁数は固定しない。

MP4内部の作成日時とFinderの作成日時、変更日時を、同じ実出力開始時刻に合わせる。
提供サンプルでの検証では、`creation_time`のみでは`AVAsset.creationDate`が秒精度になった。
FFmpegの`use_metadata_tags`で`udta/meta`に追加した文字列も、同APIの小数秒精度の改善にはつながらなかった。
Appleが規定するmovie直下の`meta`に`com.apple.quicktime.creationdate`を配置すると、小数秒を含む日時を読み戻せた。
制御下の出力に対して末尾の`moov`を確定する処理を設ける案とし、映像データを再書き出ししない。
一般のMP4を無条件に書き替える処理にはしない。
内部タグの文字列が合っているだけでは成功にしない。
形式上の秒精度への丸めが必要なら、最大誤差と丸め規則を検証結果に明記する。
ソースの日時は変更しない。

GPXは元ファイルをそのまま利用し、クリップごとのGPXを新たに作らない。
別の場所へ受け渡す場合は、選択した元GPXと同名の`.marks.csv`を`Tracks`フォルダへ1回コピーできるようにする。
これは標準ファイルの配置であり、Chrovaに新しい読み込み形式を要求しない。
出力したクリップの開始時刻がGPX範囲外なら、Chrovaでの期間選択から外れる可能性を示す。
GPXに合わせるために映像の撮影日時を偽らない。

## 保存と失敗時の扱い

編集状態はアプリのメモリ上だけに保持し、終了時に破棄する。
プロジェクト書類の保存・読み込みは提供せず、起動時は直接編集画面を開く。
終了時に編集状態の保存確認は行わない。
ウィンドウの表示位置・サイズと動画・操作エリアの高さはアプリの設定として保存する。復元時は現在の画面内に収め、ウィンドウメニューから初期サイズ・表示位置・分割位置をリセットできるようにする。
素材の参照先、時刻補正、候補範囲、書き出し先は起動中だけ保持する。
原本は変更せず、書き出した動画と来歴のmanifestは出力先に残す。
重い解析結果は再生成可能なキャッシュとし、素材が変更されたら無効化する。
書き出し開始時の計画を固定し、その後の画面の編集が実行中の処理に混ざらないようにする。

書き出しは出力先と同じボリュームの一時ファイルへ行う。
メディアと日時の検証後に、既存名を上書きしない操作で確定する。
書き出し直前の存在確認だけでは競合を防げないため、確定操作も排他的に行う。
1クリップ単位で結果を確定し、中断時も完了済みのクリップは残す。
不足容量、入力の取り外し、未対応ストリーム、検証不一致は原因と対象候補を表示し、再試行できるようにする。

## 実装を始める順序と確認方法

| 段階 | 作るもの／確認すること | 通過条件 |
| --- | --- | --- |
| 1 | Ace Pro 2の素材調査 | 同一録画の分割規則、全ストリーム、日時の意味、PTS/DTSと境界が記録される |
| 2 | 単一ファイルのlibav切り出し | コピーした範囲と元の対応が分かり、映像と音声の再圧縮がなく、追加ストリームを失わない |
| 3 | 境界をまたぐ切り出し | 境界前後で欠落、重複、A/V同期の変化がなく、日時をAVFoundationで読み戻せる |
| 4 | フォルダ読込と編集画面 | 1本の録画として再生し、マークから候補を作り、端点操作とUndoができる |
| 5 | 書き出し管理 | キャンセル、同名競合を扱い、完了した動画とmanifestを出力先に残す |
| 6 | Chrovaへの受け渡し | 出力MP4の撮影時刻が実映像と一致し、元GPXの期待する測位点に対応する |

ロジックテストは可変桁連番、欠番、別フォルダの同名、範囲外マーク、日付またぎ、正負補正、DSTの曖昧な時刻、境界の半開区間、重複候補とUndoを含める。
CSVは小数秒、カンマ、引用符、改行入りコメントを検証する。
素材テストはBフレームを含む素材、境界前後、先頭と末尾、複数音声、追加データ、非ゼロ開始PTSを対象にする。
パケットの内容は、コンテナ変更で表現が変わる場合を区別して比較する。
同じコーデック名であることだけを無劣化の証拠にしない。

Chrova確認は、出力フォルダをメディアとして読み込み、元GPXのフォルダを登録し、対象トラックを選んでGPS適用候補を確認する手順とする。
期待する撮影日時と、その日時以前の最新測位点を事前に計算して比較する。
GPSを既に持つ入力では、Chrovaの欠損GPS向け処理が対象にしないことも確認する。

## 現在の検証範囲と残る入力

Swift主体のアプリとアプリ内リンクのlibavエンジンで、単一ファイルと分割録画の切り出しを実装している。
約70.4秒の単一ファイル切り出しについて、映像4,220パケットと音声3,300パケットのSHA-256一致、映像4,220フレームのデコード、AVFoundationによる内部日時の読込を確認している。
実カメラの`VID_20260911_080012_025.mp4`と026の境界をまたぐ約2秒について、プレビュー画像、映像120フレームのデコード、映像・音声の圧縮データと時刻の一致を確認している。
通常の書き出しは全フレームのデコードを行わず、圧縮データと時刻を照合する。全デコードは診断テストで明示的に指定する。
独自領域は原本ごとに未加工で別保存し、クリップの来歴をmanifestに記録する。
Final Cut ProとChrovaの実画面での確認は未実施である。

実MP4では次の読み取りを行い、元ファイルには書き込まない。
`ffprobe`は所在と版を確認してから絶対パスで実行する。

```sh
ffprobe -v error -show_format -show_streams -of json 'artifacts/fixtures/split/VID_20260911_080012_025.mp4'
ffprobe -v error -show_packets -show_entries packet=stream_index,pts,dts,duration,flags,pos -of json 'artifacts/fixtures/split/VID_20260911_080012_025.mp4'
```

このサンプルから得た結果を、別モードや別ファームウェア、他の分割ファイルにも成立すると一般化しない。
設計書の完成と、製品が動くことの確認を分けて記録する。

## 関連する実装と検証

- [マークとGPXの読み込み](Sources/VromaCutCore/ReviewWorkflow.swift)
- [動画の日時とGPX](Sources/VromaCutCore/MediaImport.swift)
- [時刻の補正](Sources/VromaCutCore/ClockShift.swift)
- [書き出し](Sources/VromaCutCore/MediaEngine.swift)
- [素材の構造](experiments/SAMPLE_FORMAT.md)

設計時にはVroma、Chrova、VTouchの仕様と実装も参照した。
それらの別リポジトリは、このアプリのビルドには必要ない。
